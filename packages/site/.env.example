# Use `npx auth` to generate. Read more: https://cli.authjs.dev
AUTH_SECRET=
# The callback URL after signing in, only if it is behind a reverse proxy or container.
# This should be the URL of the server + `/api/auth`.
# AUTH_URL=

# The Auth Information. 
## The Client ID of the CRS app.
CLIENT_ID=8c9c574f-80c4-47c5-9119-80a519336f2a
## The Client Secret of the CRS app.
## It is intentionally left blank in the example for security. 
CLIENT_SECRET=

# The URL of the server, for both server and client to use.
# The URL on server side and client side can be different depending on deployment.
## The URL of the server on server side, i.e., in SSR or API routes.
SERVER_SERVER_URL=http://localhost:30000/
## The URL of the server on client side, i.e., in the browser.
CLIENT_SERVER_URL=http://localhost:30000/